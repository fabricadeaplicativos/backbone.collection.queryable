define(['backbone.collection.queryable'], function (BackboneCollectionQueryable) {

    var msg = 'Main demo of the backbone.collection.queryable module running!';

	console.log(msg);

	alert(msg);
});
